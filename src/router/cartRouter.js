import express from 'express';
import {
    Router
} from 'express';
import productsCollection from '../models/products.js';
import {
    uploader
} from "../utils.js";

const router = Router();

export default router;